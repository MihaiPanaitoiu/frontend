import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mihaipanaitoiu.ro',
  appName: 'CookingApp',
  webDir: 'dist/frontend',
  bundledWebRuntime: false
};

export default config;
